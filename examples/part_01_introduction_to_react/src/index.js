import React from 'react'
import ReactDOM from 'react-dom/client'

import AppSimpleState from './AppSimpleState'
import AppComplexState from './AppComplexState'

ReactDOM.createRoot(document.getElementById('root')).render(<AppComplexState />)